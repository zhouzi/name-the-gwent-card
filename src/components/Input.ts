import styled from "styled-components";

export default styled.input`
  font: inherit;
  color: inherit;
  font-size: 1.2rem;
  background: transparent;
  border: 0;
  padding: 1rem 0;
  width: 100%;
  outline: none;

  ::placeholder {
    color: #989789;
  }
`;
